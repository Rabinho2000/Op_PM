fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0.36,<3.0
alembic>=1.13,<2.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
# Validação de token OIDC (Microsoft Entra ID) — Fase 1. RS256 exige o
# extra [crypto] (cryptography), usado tanto pelo validador real (JWKS)
# como pelo mock de testes (chave de teste local, nunca uma credencial
# real — ver app/security/entra_auth.py).
pyjwt[crypto]>=2.9,<3.0
# Base de dados de fusos horários IANA para `zoneinfo` — necessária no
# Windows (e em alguns Linux mínimos), que não trazem os dados de tz do
# sistema. Usado por app/utils/timezones.py para "esta semana" no
# dashboard usar sempre Europe/Lisbon, independentemente do fuso do
# servidor onde a app corre.
tzdata>=2024.1
